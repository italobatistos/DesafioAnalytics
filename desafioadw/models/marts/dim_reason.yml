
version: 2

models:
  - name: dim_reason
    description: Dimension with reason sales data.
    columns:
      - name: sk_sales_reason
        description: Table surrogate key.
        tests:
          - unique
          - not_null

      - name: id_sales_reason
        description: Sales reason identification table.
        tests:
      
      - name: id_sales_order
        description: Sales order identification table.

      - name: reason_name
        description: Reason name table.

      - name: reason_type
        description: Reason type table.
